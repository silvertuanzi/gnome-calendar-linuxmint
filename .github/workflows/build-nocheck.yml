name: Build Arch Package

on:
  push:
    tags:
      - 'v*'
  
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Build inside Arch docker
        run: |
          docker run --rm -v "$PWD:/src" archlinux/archlinux:base-devel bash -c "
            pacman -Syu --noconfirm dconf evolution-data-server geoclue glib2 glibc graphene gtk4 libgcc libadwaita hicolor-icon-theme libedataserverui4 libgweather-4 libsoup3 libical git glib2-devel meson --needed &&
            cp /src/.github/makepkg.conf /etc/makepkg.conf.d/ &&
            useradd -m builder && chown -R builder:builder /src &&
            su - builder -c 'cd /src && makepkg --nocheck -C'
          "

      - name: Upload pkg to release
        uses: softprops/action-gh-release@v3
        with:
          files: ./*.pkg.tar.zst
          overwrite_files: true
          body: "Automated build for tag ${{ github.ref_name }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
